*.iml
.gradle
/local.properties
.DS_Store
/build
/captures
.externalNativeBuild
.kotlin

# IDE state. Machine-specific and regenerated on open, so none of it is worth tracking.
.idea/

# Signing material never belongs in the repo - the keystores live outside it, and their
# passwords live in ~/.gradle/gradle.properties.
*.jks
*.keystore
*.pepk
